type FunctionArgument
    name [UInt8]
    type OperandType
    mutable Bool

func toStrings(arguments [FunctionArgument]) [[UInt8]]
    attributes
        alwaysInline
    parse _, argument <- arguments
        result += argument.name
